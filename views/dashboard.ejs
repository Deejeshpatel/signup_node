<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        body {
            background-color: #eef2f3;
        }

        .sidebar {
            height: 100vh;
            background-color: #212529;
            padding: 20px;
            border-right: 1px solid #343a40;
        }

        .sidebar h2 {
            color: #ff9800;
            font-weight: bold;
            margin-bottom: 30px;
        }

        .sidebar a {
            color: #ffffff;
            text-decoration: none;
            margin: 10px 0;
            display: block;
            font-size: 1.1rem;
            transition: color 0.3s;
            padding: 10px;
            border-radius: 5px;
        }

        .sidebar a:hover {
            background-color: #ff9800;
            color: #fff;
        }

        .main-content {
            padding: 30px;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0px 5px 20px rgba(0, 0, 0, 0.1);
            margin-left: 20px;
        }

        .welcome-message {
            margin-bottom: 30px;
            font-weight: bold;
            font-size: 2rem;
            color: #343a40;
        }

        .info-section {
            margin-top: 20px;
            background: linear-gradient(to right, #f9f9f9, #ffffff);
            border-radius: 12px;
            padding: 20px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
        }

        .info-section h5 {
            color: #343a40;
            margin-bottom: 15px;
        }

        .info-section p {
            color: #343a40;
            font-size: 1.1rem;
        }
    </style>
</head>

<body>

    <div class="d-flex">
        <div class="sidebar">
            <h2>Admin Panel</h2>
            <a href="/profile-view">Profile</a>
            <a href="#">User Management</a>
            <a href="#">Analytics</a>
            <a href="#">Settings</a>
            <a href="#">Notifications</a>
            <a href="/#">Reports</a>
            <a href="/logout">Logout</a>
        </div>

        <div class="main-content flex-grow-1">
            <div class="container">
                <div class="welcome-message">
                    Welcome, <%= user.name %>
                </div>

                <!-- Information Section -->
                <div class="info-section">
                    <h5>Dashboard Overview</h5>
                    <p>Here you can manage your admin settings, view user statistics, and analyze the performance of
                        your application.</p>
                    <p>Explore the various options in the sidebar to get started!</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap Scripts -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>